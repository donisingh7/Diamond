import { LoginPresentation } from "@/components/shared/login-presentation";
export const metadata = { title: "Player access · Diamond" };
export default function LoginPage() { return <LoginPresentation />; }
